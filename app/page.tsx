import Image from "next/image";
import { Metadata } from "next";
import Footer from "./components/layout/Footer";
import { Roboto } from "next/font/google";
import LoginComponent from "./components/layout/LoginComponent";
import { getMembersData } from "./api/utils/helpers";
export const metadata: Metadata = {
  title: "Welcome - Filipizen",
  description: "Generated by create next app",
}

const roboto = Roboto({ 
  weight: ["400", '500', '700'], 
  subsets: ["latin"]  
});

export default async function Home() {
  const memberData = await getMembersData();
  return (
    <main className={`relative flex min-h-screen flex-col items-center justify-between ${roboto.className}`}>
      <LoginComponent memberData={memberData} />
      <Footer />
    </main>
  );
}
